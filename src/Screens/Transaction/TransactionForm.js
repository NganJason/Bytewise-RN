import { SafeAreaView } from 'react-native-safe-area-context';

import BaseText from '../../Components/BaseText';

const TransactionForm = () => {
  return (
    <SafeAreaView>
      <BaseText>TransactionForm</BaseText>
    </SafeAreaView>
  );
};

export default TransactionForm;
